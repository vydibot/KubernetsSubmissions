apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: image-pvc
  namespace: project
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi