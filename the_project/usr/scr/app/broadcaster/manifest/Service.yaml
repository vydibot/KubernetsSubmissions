apiVersion: v1
kind: Service
metadata:
  name: todo-broadcaster-svc
  namespace: project
spec:
  selector:
    app: todo-broadcaster
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
